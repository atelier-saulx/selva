import { Connection } from './connection'
import { SelvaClient } from '.'

export default (selvaClient: SelvaClient, connection: Connection) => {
  console.log('go somethign is hard dc arrived at selva client!')

  console.log('snurfy patns')
}
