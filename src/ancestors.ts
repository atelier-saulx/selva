import { Id } from './schema'
import { SelvaClient } from './'

export async function removeFromAncestors(client: SelvaClient, id: Id[]) {
  // its about adding or remving parents
}

// start with this
export async function addToAncestors(client: SelvaClient, id: Id[]) {
  // its about adding or remving parents
}
